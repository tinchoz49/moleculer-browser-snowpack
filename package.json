{
  "name": "moleculer-browser-snowpack",
  "description": "moleculer-browser running with Snowpack with full ESM support",
  "version": "0.0.1",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "dependencies": {
    "@geut/moleculer-browser": "0.0.9"
  },
  "devDependencies": {
    "snowpack": "^2.13.0"
  }
}
